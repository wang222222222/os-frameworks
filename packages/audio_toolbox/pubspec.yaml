name: audio_toolbox_framework
description: ffi bindings for the AudioToolbox Framework
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  core_foundation_framework:
    git:
      url:  https://github.com/Jordan-Nelson/os-frameworks.git
      path: packages/core_foundation
  ffi: ^2.0.1

dev_dependencies:
  ffigen: ^6.0.1
  test: ^1.16.0
